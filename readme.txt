Instalar NgModel para los popups:
  1. Usar el comando: npm config set legacy-peer-deps true
  2. Usar: ng add @ng-bootstrap/ng-bootstrap

Instalaciones de npm (unicamente por si dan errores, ya deberia de tener todo el sistema los npm funcionando):
  1. material-moment-adapter: npm i @angular/material-moment-adapter
